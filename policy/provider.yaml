apiVersion: externaldata.gatekeeper.sh/v1alpha1
kind: Provider
metadata:
  name: trivy-provider
spec:
  proxyURL: http://trivy-provider.default:8090/validate
  timeout: 1
  failurePolicy: Fail
  maxRetry: 1
